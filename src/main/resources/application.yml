quarkus:
  datasource:
    db-kind: postgresql
    username: ncapp
    password: ncapp
    jdbc:
      url: jdbc:postgresql://localhost:5432/hibernate_db
  hibernate-orm:
    database:
      generation: update